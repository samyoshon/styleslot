<!DOCTYPE html>
<html>
<head>
    <title>Jobs on Ave | Fashion jobs only</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <script type="text/javascript" src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0"/>
</head>

<body>
   <nav class="navbar navbar-inverse nav-bar" role="navigation">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle btn-navbar" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false" aria-controls="navbar" id="navbar-hamburger">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span> 
            </button>
                <%= link_to image_tag("logo.png", border: 0), root_path, id: :"navbar-logo" %>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <% if company_signed_in? %>
                    <li>
                        <%= link_to "Search Jobs", posts_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <%= link_to "Post Job", new_post_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <% if current_company.name == nil || current_company.name == "" %>
                            <%= link_to edit_company_registration_path, :method => :get, class: :"btn-navbar", id: :"btn-words" do %>
                                Edit Profile <i class="fa fa-cog"></i>
                            <% end %>
                        <% else %>
                            <%= link_to edit_company_registration_path, :method => :get, class: :"btn-navbar", id: :"btn-words" do %>
                                <%= current_company.name %> <i class="fa fa-cog"></i>
                            <% end %>
                        <% end %>
                    </li>
                <% elsif user_signed_in? %>
                    <li>
                        <%= link_to "Search Jobs", posts_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <% if current_user.name == nil || current_user.name == "" %>
                            <%= link_to edit_user_registration_path, :method => :get, class: :"btn-navbar", id: :"btn-words" do %>
                                Edit Profile <i class="fa fa-cog"></i>
                            <% end %>
                        <% else %>
                            <%= link_to edit_user_registration_path, :method => :get, class: :"btn-navbar", id: :"btn-words" do %>
                                <%= current_user.name %> <i class="fa fa-cog"></i>
                            <% end %>
                        <% end %>
                    </li>
                <% else %>
                    <li>
                        <%= link_to "Search Jobs", posts_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <%= link_to "Post Job", new_post_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                <% end %> 
            </ul>
        </div>
    </nav>

    <div class="main-body">
        <div class="home-flash">
            <% if notice %>
                <div class="bg-banner bg-success"><%= notice %></div>
            <% end %>
            <% if alert %>
                <div class="bg-banner bg-danger"><%= alert %></div>
            <% end %>
        </div>
        <%= yield %>
    </div>
</body>

</html>
