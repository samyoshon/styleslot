<!DOCTYPE html>
<html>
<head>
    <title>Styleslot</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <script type="text/javascript" src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
</head>

<body>
<!--     <nav class="nav-bar">
        <div>
   		   <li><%= link_to "Sign In", edit_company_registration_path %></li>
           <li><%= link_to "Employers", new_company_session_path %></li>
        </div>
   </nav>
 -->
   <nav class="navbar navbar-inverse nav-bar" role="navigation">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle btn-navbar" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false" aria-controls="navbar" id="navbar-hamburger">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span> 
            </button>
<!--                 <%= link_to image_tag("logo.png", border: 0), root_path, id: :"navbar-logo" %> -->
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <% if user_signed_in? %>
                    <li>
                        <%= link_to "Search Jobs", posts_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <%= link_to "Post Job", new_post_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li class="dropdown-toggle btn-navbar" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">

                    </li>
                    <ul class="dropdown-menu">
                        <li>
                            <%= link_to "Edit Profile", edit_user_registration_path, class: :"btn-navbar", id: :"btn-words" %>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <%= link_to "Logout", destroy_user_session_path, :method => :delete, class: :"btn-navbar", id: :"btn-words" %>
                        </li>
                    </ul>
                <% else %>
                    <li>
                        <!-- posts_path -->
                        <%= link_to "Search posts", posts_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                    <li>
                        <%= link_to "Employers", new_post_path, :method => :get, class: :"btn-navbar", id: :"btn-words" %>
                    </li>
                <% end %> 
            </ul>
        </div>
    </nav>

    <div class="main-body">
        <div class="home-flash">
            <% if notice %>
                <div class="bg-success"><%= notice %></div>
            <% end %>
            <% if alert %>
                <div class="bg-danger"><%= alert %></div>
            <% end %>
        </div>
        <%= yield %>
    </div>
</body>
</html>
